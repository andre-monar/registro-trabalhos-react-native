import { View, Text } from 'react-native';
import styles from '../styles';

export default function TrabalhosScreen() {
    return (
        <View style = {styles.container}>
            <Text>Tela de trabalhos</Text>
        </View>
    );
}